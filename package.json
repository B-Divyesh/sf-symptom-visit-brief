{
  "name": "symptom-visit-brief",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/build-sw.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "npm run build && playwright test",
    "check": "tsc --noEmit && npm test && npm run build"
  },
  "dependencies": {
    "jspdf": "4.2.1"
  },
  "overrides": {
    "playwright-core": "1.58.2"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.58.2",
    "@types/node": "22.13.4",
    "typescript": "5.7.3",
    "vite": "^6.4.3",
    "vitest": "^3.2.7"
  }
}
